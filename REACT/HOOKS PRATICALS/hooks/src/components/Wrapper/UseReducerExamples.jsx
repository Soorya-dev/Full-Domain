import React from 'react';
import UseReducer from '../UseReducer';  // Import the UseReducer component

const UseReducerExamples = () => {
  return (
    <>
      <UseReducer /> {/* Render the UseReducer component */}
    </>
  );
};

export default UseReducerExamples;
